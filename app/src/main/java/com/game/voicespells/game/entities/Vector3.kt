package com.game.voicespells.game.entities

/**
 * Represents a 3D vector or a point in 3D space.
 */
data class Vector3(var x: Float = 0f, var y: Float = 0f, var z: Float = 0f) {
    // You can add common vector operations here later if needed,
    // e.g., addition, subtraction, magnitude, normalization.
}
